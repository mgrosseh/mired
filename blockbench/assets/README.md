Open create mod jar with an archive viewer (7zip, winrar, ark) and extract the contents of the assets directory into this assets directory.
Then repeat with minecraft 1.21.1 jar, copying its assets into this.

Make sure that `create-1.21.1-6.0.10.jar/assets/create` becomes `assets/create` (this assets) and not e.g. `assets/create-1.21.1-6.0.10/assets/create` (same with minecraft).